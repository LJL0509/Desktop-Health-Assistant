# Changelog

## v1.0.2 - 2026-08-14

本次更新修复高 DPI 环境下健康弹窗按钮被裁切的问题，并避免用户在持续保持异常姿势或低眨眼频率时只收到一次提醒。

### Changed

- 姿势和眨眼弹窗不再使用固定高度，改为完成控件布局后按实际需求计算窗口高度；
- 窗口保留最小高度和底部安全空间，使“知道了”按钮在当前 Windows DPI 缩放下完整显示；
- 头部前倾或头部过低首次提醒后仍未恢复，3 分钟后再次提醒，此后每 10 分钟提醒；
- 低眨眼频率采用相同的 3 分钟和 10 分钟重复提醒节奏；
- 眨眼重复提醒只累计有效人脸观察时间，脸部缺失和摄像头暂停期间不推进计时；
- 姿势或眨眼恢复正常后立即清除重复提醒计划，下次异常重新执行首次提醒流程。

### Validated

- 使用姿势提醒和眨眼提醒的真实中文文案分别完成当前 DPI 环境截图检查，文字和按钮均完整可见；
- 首次提醒、3 分钟第二次提醒、此后 10 分钟重复提醒和恢复重置边界测试通过；
- 脸部缺失时暂停眨眼重复提醒计时的测试通过；
- 138 项自动测试、Python 编译检查和补丁格式检查全部通过。

## v1.0.1 - 2026-08-14

本次更新的目的是把只能在开发环境运行的项目整理为可以直接交给其他 Windows 用户使用的软件，同时保留现有监测算法和本地隐私边界。

### Added

- PyInstaller 一目录发布配置，打包 Python、MediaPipe、模型、Tkinter、通知组件和运行库；
- 可直接解压运行的 64 位 Windows 便携包；
- 应用程序与托盘使用的心形健康图标；
- EXE 产品名称、文件说明和版本信息；
- `%LOCALAPPDATA%\Desktop Health Assistant\data` 用户数据目录；
- `application.log` 文件日志和意外退出错误提示；
- 托盘中的开机自动启动控制和数据目录入口；
- Inno Setup 安装器配置和 Windows 自动构建脚本；
- 应用路径、运行时日志和开机启动相关自动测试。

### Changed

- 模型和版本文件在打包模式下从应用资源目录读取，开发模式仍使用项目目录；
- 重复启动在打包模式下显示 Windows 提示框；
- 启动时启用 Windows DPI 感知，改善托盘菜单和文字清晰度；
- 构建脚本直接读取 `VERSION` 生成发布文件名，避免多处版本号不一致；
- 当前姿势、眨眼、补水和报告逻辑保持不变。

### Validated

- 132 项自动测试全部通过；
- Python 编译、依赖完整性和 Git 补丁检查通过；
- 打包程序成功加载 MediaPipe、模型和内置摄像头并持续运行；
- 便携包在全新解压目录以无摄像头模式完成冷启动；
- EXE 版本信息、单实例保护和用户数据目录写入通过验证；
- 用户完成预览版实际使用测试并确认表现符合预期。

### Known limitations

- 当前程序未进行数字签名，Windows SmartScreen 可能显示未知发布者；
- 安装器配置已完成，但本机未安装 Inno Setup，因此本次只生成便携包；
- 尚未在多种型号的其他电脑和摄像头上完成兼容性测试。

## v1.0.0 - 2026-08-12

第一个初级可用版本。冻结当前检测算法，将姿势、眨眼、健康提醒、自动日报和系统托盘整合为一个可长期运行的桌面助手。

### Added

- Windows 系统托盘入口；
- 隐藏或恢复摄像头监测窗口；
- 暂停监测并释放摄像头，以及重新打开摄像头继续监测；
- 从托盘打开最新日期的 Markdown 健康报告；
- 从托盘正常退出并结算监控和连续使用会话；
- 完整摄像头模式与无摄像头后台模式的双击启动脚本；
- 7 项托盘状态、报告选择和统一入口测试，总自动测试数增加到 111 项。

### Changed

- 版本从实验阶段进入 `v1.0.0` 初级可用阶段；
- 明确隐藏窗口不会停止摄像头监测，暂停监测才会释放摄像头；
- 托盘线程只提交控制状态，OpenCV 和 MediaPipe 对象继续由监测主线程操作；
- 当前算法与提醒阈值冻结，后续修改进入新版本。

### Validated

- 111 项自动测试全部通过；
- Python 编译检查和依赖完整性检查通过；
- 真实 Windows 托盘线程可以启动并请求后台模式正常退出；
- 托盘退出后连续使用会话正常写入结束事件；
- 已有昨日报告在启动时未被覆盖；
- 完整摄像头模式可通过统一入口持续运行。

### Deferred

- 屏幕距离监测；
- 固定姿势持续时间识别；
- 肩膀经常离开画面时的圆肩识别；
- 开机自启动和应用内报告历史界面。

## v0.7.0 - 2026-08-12

第七个可验证版本，自动生成前一天健康报告，并正确处理跨午夜运行的数据边界。

### Added

- 自动日报调度器；
- 第二天首次启动时补生成缺失的昨天报告；
- 程序运行跨过午夜时自动结算并生成前一天报告；
- 摄像头模式跨午夜自动拆分监控会话；
- 无摄像头模式跨午夜自动拆分连续使用会话；
- 报告存在性检查，同时存在 JSON 和 Markdown 才视为已完成；
- 6 项自动日报、无数据措辞和午夜活动边界测试，总自动测试数增加到 104 项。

### Changed

- 自动生成只处理缺失报告，不覆盖已有报告；
- 手动 `daily_report.py --date` 仍可主动重新生成指定日期；
- 摄像头跨日时保留姿势和睁眼校准，不要求重新按 `C`；
- MediaPipe 视频时间戳跨日继续递增，不随日志会话重置；
- 新一天连续使用段不会根据键鼠空闲时间回推到午夜之前；
- 无有效监测数据的报告改为“无法评价”，不再暗示当天没有健康问题；
- 日报生成失败后等待 60 秒重试，避免每秒重复写错误日志。

### Validated

- 启动补生成、已有报告不覆盖、同日无操作和跨午夜先结算后生成测试通过；
- 跨午夜回调收到准确的本地 `00:00:00` 边界时间；
- 使用真实项目日志成功补生成 2026-08-11 的 JSON 与 Markdown 报告；
- 无数据报告正确显示“当天没有有效监测数据，无法评价”；
- 无摄像头短启动确认已有报告修改时间保持不变；
- 104 项自动测试、Python 编译检查和依赖完整性检查通过。

### Known limitations

- 自动日报目前只保存本地文件，不主动弹窗或提供报告查看界面；
- 已存在的报告不会自动刷新，用户需要手动命令重新生成历史日期；
- 真实跨午夜长时间运行仍需在自然使用中继续观察；
- 程序被系统强制结束时，最后一段会话仍可能缺少结算事件，报告会标记为不完整。

## v0.6.0 - 2026-08-12

第六个可验证版本，加入个人睁眼校准、眨眼频率监测、低频提醒和日报统计。

### Added

- 左右眼几何开合度与 MediaPipe `eyeBlinkLeft/Right` 联合信号；
- 与姿势校准共用一次 `C` 操作的个人睁眼基线；
- 普通双眼眨眼、单帧高置信快速眨眼和持续闭眼状态机；
- 最近 5 分钟有效眼部观察窗口；
- 平均低于 15 次/分钟时的眼睛休息提示；
- 达到 18 次/分钟后的低频状态恢复，避免阈值附近反复提醒；
- 面部缺失和摄像头暂停时的眼部统计暂停；
- 日报中的有效观察时长、眨眼数、平均频率、低频提醒和持续闭眼统计；
- 只保留最新待处理帧的异步上身分割工作器；
- 26 项眨眼、时间戳、低频窗口和后台线程测试，总自动测试数增加到 98 项。

### Changed

- MediaPipe 视频时间戳统一保证严格递增，避免同一整数毫秒导致程序退出；
- 正式程序启用 Face Landmarker blendshape 输出；
- 联合校准至少运行 10 秒，并等待姿势和眼睛各取得 30 个有效样本，最长 20 秒；
- 校准失败时保留具体原因，眼睛已成功时明确显示仅眨眼校准状态；
- 上身 Image Segmenter 从摄像头主循环移至独立后台线程；
- 后台忙时覆盖旧待处理帧，只处理最新画面，避免姿势结果积压；
- 多次重新校准时累计保存有效眼部观察时长，避免日报平均频率失真。

### Validated

- 独立实验个人睁眼基线成功建立；
- 受控 10 次普通眨眼识别为 10 次，没有持续闭眼误判；
- 一次约 3.42 秒持续闭眼产生 1 段持续闭眼事件，普通眨眼计数为 0；
- 正式联合校准成功获得 30 个姿势样本和 159 个眼部样本；
- 正式程序普通眨眼测试为 5/5；
- 快速眨眼在同步分割版本中为 4/5，因此不再单纯放宽阈值；
- 改为异步上身分割后，用户确认程序画面流畅；
- 98 项自动测试、Python 编译检查和依赖完整性检查通过。

### Known limitations

- 极快眨眼可能完全落在两次人脸采样之间，因此仍可能偶尔漏记；
- 15 次/分钟与恢复阈值 18 次/分钟是个人产品默认值，不是国家标准或医学危险阈值；
- 当前只在一名用户、一台内置摄像头和有限光照条件下验证；
- 持续闭眼只作为行为事件记录，不用于疲劳或睡眠诊断；
- 异步分割改善了流畅度，但仍需后续自然长时间使用验证姿势与眨眼同时运行的稳定性。

## v0.5.0 - 2026-08-12

第五个可验证版本，统一完整监控与无摄像头后台模式的启动方式，并阻止重复运行。

### Added

- `desktop_health_assistant.py` 统一启动入口；
- 默认完整摄像头模式；
- `--no-camera` 补水、睡眠和连续使用后台模式；
- 基于 Windows 命名互斥量的单实例锁；
- 统一入口与两个旧入口之间共享的重复运行保护；
- 4 项单实例锁和启动参数测试，总自动测试数增加到 72 项。

### Changed

- README 首选启动命令改为统一入口；
- `reminder_service.main` 支持由统一入口传入参数；
- 旧的摄像头与提醒脚本直接运行时也会申请同一实例锁；
- 重复启动时显示中文说明并使用退出码 `2` 结束第二个进程。

### Validated

- 使用统一入口成功打开摄像头完整模式；
- 第一个实例运行期间启动 `--no-camera`，第二个实例被正确拒绝；
- 第一个实例继续正常运行，未受重复启动尝试影响；
- 按 `Q` 后写入 `session_summary` 和 `session_ended`；
- 连续使用事件以 `service_stopped` 正常结算；
- 通知后台线程完成清理后 Python 进程正常退出；
- 72 项自动测试、Python 编译检查和依赖完整性检查通过。

### Known limitations

- 当前仍通过命令行参数选择摄像头模式，尚无托盘菜单或图形设置界面；
- 旧入口仍保留用于调试，但日常使用应只运行统一入口；
- 暂未实现开机自动启动和自动生成每日报告。

## v0.4.0 - 2026-08-12

第四个可验证版本，记录连续电脑使用时长，并将离屏活动建议合并到补水提醒。

### Added

- Windows `GetLastInputInfo` 键鼠空闲时间读取；
- 连续电脑使用会话开始、结束和持续时长本地事件；
- 连续 5 分钟无输入后重置使用时长；
- 日报中的使用段数、累计使用时长和最长连续使用时长；
- 长时间连续使用时的日报改进方向；
- 7 项活动状态与日报集成测试，总自动测试数增加到 68 项。

### Changed

- 补水通知标题改为“补水与休息提示”；
- 补水文案同时建议喝水和离开电脑活动几分钟；
- 取消独立的连续使用 60 分钟休息弹窗，避免与补水通知重复打扰；
- 连续使用模块只负责统计，不记录按键内容；
- 真实久坐仍标记为未直接测量，不用键鼠活动冒充坐姿判断。

### Validated

- Windows 空闲时间接口在当前电脑成功返回空闲秒数；
- 连续使用开始、5 分钟休息重置、服务退出结算和跨日报统计测试通过；
- 合并后的“补水与休息提示”完成实际 Windows 弹窗测试；
- 用户点击“已喝水”后，测试按钮回调正确记录 250 ml，且未写入真实饮水状态；
- 68 项自动测试、Python 编译检查和依赖完整性检查通过。

### Known limitations

- 键鼠无输入不一定表示用户离开电脑，看视频或阅读时可能低估连续使用时间；
- 键鼠活动只能估算电脑交互，不能证明用户一直坐着；
- 5 分钟重置是可调整的产品默认值，不是医学危险阈值；
- 同时运行摄像头主程序和独立提醒服务可能产生重复统计，正式桌面入口尚未整合。

## v0.3.0 - 2026-08-12

第三个可验证版本，加入独立于摄像头的生活提醒和每日健康报告。

### Added

- 默认每 60 分钟一次的补水提醒；
- 补水通知中的“已喝水”按钮和默认 250 ml 本地记录；
- 本机时间每天凌晨 1 点一次的睡眠提示；
- 可独立运行、无需开启摄像头的提醒服务；
- 监控程序中的可选 `W` 手动喝水记录；
- 每日 JSON 数据报告和中文 Markdown 可读报告；
- 姿势问题累计时长、次数、最长一次和提醒次数汇总；
- 监控运行时长、摄像头暂停、数据缺失和覆盖率汇总；
- 补水次数、估计总量、连续未确认提醒和睡眠提醒汇总；
- 不完整会话、日志估算和未结束姿势事件的数据质量说明；
- 4 项日报测试，总自动测试数增加到 61 项。

### Changed

- Windows 通知改用可交互提醒场景和长时显示，确保通知横幅弹出；
- 测试通知动作与真实补水状态隔离；
- 监控会话开始时写入明确的 `session_started` 事件；
- 补水措辞使用“长时间未记录喝水”，不声称摄像头识别喝水行为；
- 提醒调度在摄像头暂停期间仍继续运行。

### Validated

- 补水与睡眠通知均完成 Windows 实际弹窗测试；
- “已喝水”按钮回调正确记录 250 ml 测试动作；
- 睡眠提示文案和中文编码检查通过；
- 使用 2026-08-12 真实监控与补水日志成功生成日报；
- 日报正确排除测试通知，并避免重复统计监控日志中的喝水镜像事件；
- 61 项自动测试、Python 编译检查和依赖完整性检查通过。

### Known limitations

- 补水状态依赖用户点击“已喝水”或手动记录，不能自动知道是否实际喝水；
- 最长未确认喝水时长按连续补水提醒估算；
- 每日报告目前由命令手动生成，尚未自动定时生成或提供图形界面；
- 连续久坐、固定姿势、眨眼频率和屏幕距离尚未接入日报；
- 监控程序被强制关闭时，最后一段会话和姿势事件可能低估，报告会明确标注。

## v0.2.0 - 2026-08-10

第二个可验证版本，使用人脸与可见上身轮廓重新实现脖子前伸监控。

### Added

- MediaPipe Image Segmenter 上身轮廓提取；
- `PARTIAL`、`FULL`、`TOO LOW` 和数据不足可见状态；
- 脸宽、上身轮廓宽度、脸/上身比例、垂直变化和轮廓面积联合判断；
- 整体靠近和整体远离的正常动作判断；
- 头部过低的独立提示；
- 头部前伸连续 60 秒后的提醒计时，带 3 秒恢复宽限期；
- 姿势事件开始、结束、持续时长、提醒次数和会话汇总日志；
- 头部前伸候选状态和持续数据不可靠提示；
- 46 项自动测试。

### Changed

- 正常姿势校准由肩膀/虚拟锚点改为脸部与可见上身轮廓；
- 校准时间由 15 秒调整为 10 秒；
- 监控窗口由 5 秒调整为最近 2 秒中位数；
- 健康基线只由用户校准产生，运行中不再自动覆盖；
- 头部前伸不再要求脸一定比初始校准时更大；
- `UNCERTAIN` 和严重轮廓缺失不再显示为正常；
- 屏幕距离继续与脖子姿势保持独立。

### Validated

- 两轮受控动作序列共 12 组，正常、整体靠近和头部前伸回放结果为 12/12；
- 第一轮持续监控暴露 9 次漏检，修正后旧日志回放为 7 次头部前伸、1 次数据不足、1 次仍无法区分；
- 第二轮持续监控获得 14 次人工反馈，其中 11 次正确、3 次正常姿势误报；
- 整体靠近、整体远离以及不同距离下的头部前伸均完成实际验证；
- 所有实验仍只保存数值和事件，不保存摄像头图片或视频。

### Known limitations

- 舒服坐姿下可见上身轮廓明显变窄时，可能把正常姿势误报为头部前伸；
- `PARTIAL` 与 `FULL` 边界附近的轮廓宽度会随衣服和画面裁切变化；
- 当前正面单目特征无法区分少量外观几乎相同的正常与头部前伸样本；
- 60 秒提醒时长是可调整的产品默认值，不是医学危险阈值；
- 尚未实现 Windows 系统通知、补水提醒、睡眠提示和每日健康报告界面。

## v0.1.0 - 2026-08-10

第一个可验证版本，完成脖子前倾持续监控原型。

### Added

- Windows、Python、MediaPipe 和摄像头共享能力检查；
- MediaPipe Face Landmarker 与 Pose Landmarker 模型下载脚本；
- 人脸、眼睛、头部和肩部关键点预览；
- 肩部关键点画外推测的有效性检查；
- 正常、头部前移和整体靠近三组采样实验；
- 肩膀不可见时的虚拟头肩锚点；
- 15 秒个人姿势校准和 5 秒窗口平滑；
- `NECK NORMAL` 与 `NECK FORWARD` 二态监控；
- 3 秒状态确认；
- 数据不足持续 10 秒后的应用内提醒；
- 本地状态日志和人工反馈记录；
- 18 项自动测试。

### Validated

- 摄像头 Media Foundation 双路共享测试通过；
- 受控持续监控测试获得 25 次人工反馈，23 次正确；
- 数据不足提醒 3 次触发、3 次恢复；
- 所有实验只保存数值，不保存摄像头画面。

### Known limitations

- 8% 判断阈值是个人实验值，不是医学标准；
- 轻微头部前移可能漏检；
- 尚未进行长时间自然使用验证；
- 尚无系统通知、眨眼、饮水、久坐和每日报告功能。
